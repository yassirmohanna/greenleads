<%- include('partials/header', { title: 'Support' }) %>

<h1>Support</h1>
<p class="muted">Contact: <a href="mailto:<%= contactEmail %>"><%= contactEmail %></a></p>

<div class="card">
  <h2>Rules & Guidelines</h2>
  <ul>
    <li>Access is invite-only and may be revoked by the owner.</li>
    <li>GreenLeads does not scrape or automate Nextdoor browsing.</li>
    <li>Only your own Nextdoor notification emails or manual imports are used.</li>
    <li>Leads are private and used solely for your internal business needs.</li>
  </ul>
</div>

<div class="card">
  <h2>Quick Answers</h2>
  <form method="post" action="/support">
    <label>Ask a question</label>
    <input type="text" name="question" value="<%= question %>" placeholder="How do I get access?" />
    <button type="submit">Get answer</button>
  </form>
  <% if (answer) { %>
    <p class="muted">Answer:</p>
    <p><%= answer %></p>
  <% } %>
</div>

<%- include('partials/footer') %>
